# apex-dashboard

apex-dashboard is a personal finance dashboard UI built with React, Vite, and Tailwind CSS.

## Getting Started

```bash
npm install
npm run dev
```

## Scripts

```bash
npm run dev
npm run build
npm run lint
npm run preview
```

## Environment Variables

Copy `.env.example` to `.env` for local settings. Real `.env` files are ignored by Git.
